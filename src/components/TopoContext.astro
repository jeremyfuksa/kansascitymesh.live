---
/**
 * Always-visible topographic context callout for coverage explanations.
 * Provides educational context about terrain and elevation advantages.
 */
interface Props {
  title?: string;
  icon?: string;
}

const { title = "Topographic Context", icon = "🗺️" } = Astro.props;
---

<aside class="bg-primary-600/5 border-l-4 border-primary-600 rounded-r-lg p-4 mb-6">
  <div class="flex items-center gap-2 mb-3">
    <span class="text-2xl" aria-hidden="true">{icon}</span>
    <span class="text-sm font-bold uppercase tracking-wide text-primary-700">{title}</span>
  </div>
  <div class="text-sm text-neutral-700 leading-relaxed">
    <slot />
  </div>
</aside>
