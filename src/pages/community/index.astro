---
import { getEntry } from "astro:content";
import BasePage from "../../layouts/content/BasePage.astro";

const entry = await getEntry({ collection: "pages", slug: "community" });
if (!entry) throw new Error("Missing content entry: pages/community");
---

<BasePage entry={entry} />
