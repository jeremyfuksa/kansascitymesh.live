---
import { getEntry } from "astro:content";
import ArticlePage from "../../layouts/content/ArticlePage.astro";

const entry = await getEntry({ collection: "pages", slug: "resources/troubleshooting" });
if (!entry) throw new Error("Missing content entry: pages/resources/troubleshooting");
---

<ArticlePage entry={entry} />
