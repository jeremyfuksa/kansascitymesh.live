---
import { getEntry } from "astro:content";
import ArticlePage from "../../layouts/content/ArticlePage.astro";

const entry = await getEntry({ collection: "pages", slug: "resources/index" });
if (!entry) throw new Error("Missing content entry: pages/resources/index");
---

<ArticlePage entry={entry} />
