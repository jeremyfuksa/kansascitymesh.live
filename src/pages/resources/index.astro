---
import { getEntry } from "astro:content";
import ArticlePage from "../../layouts/content/ArticlePage.astro";

const entry = await getEntry({ collection: "pages", slug: "resources" });
if (!entry) throw new Error("Missing content entry: pages/resources");
---

<ArticlePage entry={entry} />
