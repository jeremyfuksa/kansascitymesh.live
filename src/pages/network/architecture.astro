---
import { getEntry } from "astro:content";
import ArticlePage from "../../layouts/content/ArticlePage.astro";

const entry = await getEntry({ collection: "pages", slug: "network/architecture" });
if (!entry) throw new Error("Missing content entry: pages/network/architecture");
---

<ArticlePage entry={entry} />
