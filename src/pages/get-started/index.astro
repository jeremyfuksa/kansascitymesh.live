---
import { getEntry } from "astro:content";
import BasePage from "../../layouts/content/BasePage.astro";

const entry = await getEntry({ collection: "pages", slug: "get-started/index" });
if (!entry) throw new Error("Missing content entry: pages/get-started/index");
---

<BasePage entry={entry} />
