---
import { getEntry } from "astro:content";
import ArticlePage from "../../layouts/content/ArticlePage.astro";

const entry = await getEntry({ collection: "pages", slug: "get-started/mqtt" });
if (!entry) throw new Error("Missing content entry: pages/get-started/mqtt");
---

<ArticlePage entry={entry} />
