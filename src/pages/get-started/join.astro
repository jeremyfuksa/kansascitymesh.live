---
import { getEntry } from "astro:content";
import ArticlePage from "../../layouts/content/ArticlePage.astro";

const entry = await getEntry({ collection: "pages", slug: "get-started/join" });
if (!entry) throw new Error("Missing content entry: pages/get-started/join");
---

<ArticlePage entry={entry} />
